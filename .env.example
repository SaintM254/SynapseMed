# Optional: prefill the Google OAuth client ID at build/dev time.
# A *web* client ID is not a secret — it only identifies the app to Google.
# Copy .env.example to .env for local development (.env is gitignored).
VITE_GOOGLE_CLIENT_ID=
